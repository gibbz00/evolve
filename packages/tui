# Syntax: Tabs allowed but comments must be written as new lines
# Optional packages:
# * github-cli: installed if GITHUB_TOKEN is set in evolve.env
#
# Packages installed regardless:
# * if using rpi4: linux-rpi raspberrypi-firmware raspberrypi-bootloader
# * paru: aur helper and pacman wrapper 
bash
    fzf
    z
    bash-completion 
    fd
# replace the following two with `man` on Arm
man-pages
man-db
tree
rsync
usbutils
tmux
helix-git
    # Language servers for helix
    rust-analyzer
    taplo
    marksman-bin
    clang
    bash-language-server
    python-lsp-server
    typescript-language-server
    nodejs-svelte-language-server
btop
# TODO: add language servers for helix; rust-analyser and marksman
